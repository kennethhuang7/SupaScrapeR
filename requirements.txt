praw==7.7.1
textblob==0.17.1
vaderSentiment==3.3.2
pytrends==4.9.2
supabase==2.7.4
python-dotenv==1.0.0
requests==2.31.0
spacy==3.7.2
inflect==7.0.0
psutil==5.9.6
urllib3<2.0

# SETUP INSTRUCTIONS FOR NEW USERS:
# 1. Install Python dependencies:
#    pip install -r requirements.txt --break-system-packages
#    (On Mac/Linux, you may need to use pip3 instead of pip)
#
# 2. Download spaCy language model:
#    python -m spacy download en_core_web_sm
#    (On Mac/Linux, you may need to use python3 instead of python)
#    or pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
#    if the commnad above does not work
#
# 3. Install Node.js dependencies:
#    npm install
#
# 4. Run the application:
#    npm run electron-dev (development mode)
#    npm run dist (production build)