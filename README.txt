## Setup Virtual Environment:

### Prerequisites
- Python 3.7 or higher

### Installation
Copy and paste the lines of code for your specified OS. 

# Windows
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt

# MacOS / Linux
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt